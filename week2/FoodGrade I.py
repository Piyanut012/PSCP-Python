"""FoodGrade I"""


def main():
    """Input"""
    def funcn_data(total):
        """Chicken"""
        total = 0
        chicken = int(input())
        if chicken >= 50 and chicken <= 70:
            total = total + 1
        return total
 
    total = funcn_data(0)
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    total = funcn_data(total) + total
    print(total)

main()
